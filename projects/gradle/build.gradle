plugins {
    id 'org.springframework.boot' version '2.7.3'
    id 'io.spring.dependency-management' version '1.0.13.RELEASE'
    id 'java'
    id 'checkstyle'
}

allprojects {
    group = System.getProperty('group')
    version = System.getProperty('version')
    sourceCompatibility = System.getProperty('sourceCompatibility')
    
    repositories {
        mavenCentral()
    }
    // Apply configuration files
    apply from: 'conf/dependencies.gradle'
    apply from: 'conf/tasks.gradle'
    apply from: 'conf/test-config.gradle'
    apply from: 'conf/quality.gradle'
}


